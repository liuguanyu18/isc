# 基于园区负荷预测的 Agentscope 可视化智能体设计

## 目标概述
面向园区管理人员构建一套可视化智能体，能够完成数据自动化分析、模型自动选择、负荷预测与结果可视化，为设备调度与能源管理提供可执行的决策支持。

## 总体架构（Root）
智能体以「数据—理解—建模—评估—可视化—反馈」闭环为核心，依托 Agentscope 的多智能体协作机制，采用“文本对话 + Skills 调用”的流程组织方式。智能体仅通过文本交流驱动各类技能执行，将原本的节点操作拆分为可复用的 Skills（工具/任务流），以完成从多源数据接入到模型推荐、训练、评估与可视化输出的全过程，并通过用户反馈持续优化模型与流程。

## Skills 化设计原则
- **文本驱动**：所有操作通过智能体文本对话触发，无需图形化操作步骤。
- **技能解耦**：将数据读取、语义解析、预处理、建模、评估等拆分为独立 Skills，便于组合与复用。
- **技能编排**：智能体根据上下文自动选择/排序/调用 Skills，实现端到端流程。
- **可解释反馈**：每次 Skill 调用均返回结构化摘要与可视化链接，便于管理人员理解。

## Skills 清单（已实现）
- `skills/data-ingestion`：数据接入
- `skills/field-semantics`：字段语义
- `skills/data-cleaning`：数据清洗
- `skills/feature-analysis`：特征分析
- `skills/model-recommendation`：模型推荐
- `skills/training-validation`：训练验证
- `skills/result-visualization`：结果可视化
- `skills/interaction-feedback`：交互反馈

## 节点设计（Nodes → Skills）

### 节点1：数据读取与连接（Skill：数据接入）
**职责**
- 连接数据库读取负荷数据
- 支持 CSV/Excel/SQL 等多源接入
- 自动识别数据格式与结构

**关键能力**
- 数据源类型识别（文件 / 数据库）
- 自动推断字段类型（数值、时间、文本）
- 统一数据表结构输出（DataFrame）

### 节点2：列名语义分析（Skill：字段语义）
**职责**
- 解析列名语义，推断字段含义
- 处理缩写或别名（如 Pwr → Power）
- 生成字段解释报告

**关键能力**
- 语义匹配词典（电力、温度、负荷、时间等）
- 多语言与缩写映射
- 结果输出为字段说明表（字段名、含义、置信度）

### 节点3：数据预处理（Skill：数据清洗）
**职责**
- 缺失值处理与补全
- 异常检测与修正
- 数据标准化与归一化

**关键能力**
- 缺失值策略选择（删除/填充/插值）
- 异常值检测（统计阈值、箱线图、Z-Score）
- 标准化/归一化策略自动推荐

### 节点4：特征工程与分析（Skill：特征分析）
**职责**
- 特征相关性分析
- 时间序列分解（趋势、季节性、残差）
- 关键变量识别

**关键能力**
- 相关系数矩阵、互信息分析
- 时间序列特征抽取（滞后项、周期项）
- 重要特征排名与解释

### 节点5：模型选择与评估（Skill：模型推荐）
**职责**
- 模型候选列表管理（ARIMA、LSTM、Prophet）
- 基于数据特征自动推荐模型
- 评估指标计算（MAE、RMSE、R²）

**关键能力**
- 模型选择规则库（线性/非线性/序列长度）
- 模型可解释性与计算成本评估
- 推荐理由与排序输出

### 节点6：模型训练与验证（Skill：训练验证）
**职责**
- 训练集/测试集划分
- 模型训练与调参
- 验证结果可视化

**关键能力**
- 时间序列滑窗切分
- 超参数搜索（网格/贝叶斯）
- 训练与测试误差曲线展示

### 节点7：结果输出与可视化（Skill：结果可视化）
**职责**
- 可视化预测结果（折线图、热力图）
- 输出误差分析报告
- 提供可操作建议

**关键能力**
- 预测区间可视化
- 误差分布图与对比
- 优化建议（负载削峰、设备调度、能源分配）

### 节点8：用户交互与反馈（Skill：交互反馈）
**职责**
- 管理员界面设计
- 用户可调整参数
- 反馈机制推动迭代优化

**关键能力**
- 交互式仪表盘（指标、告警、建议）
- 用户输入参数影响模型
- 反馈闭环与模型再训练

## 边设计（Edges → Skills 编排）
- **数据接入 Skill → 字段语义 Skill**：数据结构确定后执行语义解析
- **字段语义 Skill → 数据清洗 Skill**：依据字段含义选择清洗方式
- **数据清洗 Skill → 特征分析 Skill**：为模型构建有效输入
- **特征分析 Skill → 模型推荐 Skill**：依据特征推荐最优模型
- **模型推荐 Skill → 训练验证 Skill**：执行训练与调参
- **训练验证 Skill → 结果可视化 Skill**：生成预测结果与报告
- **结果可视化 Skill → 交互反馈 Skill**：用户调整参数与反馈
- **所有 Skills → 迭代优化循环**：形成持续优化闭环

## 交付成果（Leaves）
- 可视化预测结果（图表 + 文字说明）
- 模型选择建议报告（推荐模型与理由）
- 数据分析报告（字段语义、特征分析、异常检测）
- 用户操作指南与系统使用说明
- 优化后的模型配置与参数设置
- 园区负荷管理建议（设备调度、能源分配策略）

## 可视化智能体运行流程（简要）
1. **文本对话输入** → 智能体理解需求
2. **调用数据接入 Skill** → 自动解析结构
3. **调用字段语义 Skill** → 生成字段说明
4. **调用数据清洗 Skill** → 完成预处理
5. **调用特征分析 Skill** → 分析时序特征
6. **调用模型推荐 Skill** → 输出候选模型与排序
7. **调用训练验证 Skill** → 生成预测结果与误差分析
8. **调用结果可视化 Skill** → 图表 + 建议报告
9. **调用交互反馈 Skill** → 用户调整并触发迭代优化

## 预期收益
- 提高园区负荷预测精度
- 降低模型选择与调参成本
- 支持管理人员快速决策与能耗优化
- 形成“预测—反馈—优化”闭环
